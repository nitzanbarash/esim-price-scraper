# מערכת הכספים — הפעלה

מערכת נפרדת לגמרי מהאתר ומהבוטים שמוכרים. היא לא צריכה לרוץ כל הזמן:
היא בונה את הספרים פעם ביום, ואפשר תמיד ללחוץ "רענון" ידנית.

## מה זה עושה

- קורא את **גיליון הקבלות** — מה נמכר, למי, באיזה מחיר
- קורא את **המחירון** — מחיר המחירון של כל חבילה, כדי לדעת מי קיבל הנחה
- קורא את **תיבת האספקה** (waverolesupply) — קבלות ה-Stripe של הספק,
  שהן ההוכחה מה באמת ירד מהכרטיס
- קורא את **התיבה הראשית** (uper.request) — חשבוניות תשתית (Vercel, דומיין וכו')
- **מצליב** בין חיובי הספק למכירות ומדווח על כל חיוב שאין מולו מכירה
- מאפשר להוסיף הוצאה ידנית או **לצלם קבלה** מהטלפון והיא נסרקת לבד
- מייצא **אקסל לרואה חשבון**

---

## שני חלקים, ולמה

סקריפט של Google יכול לקרוא רק את תיבת הדואר של החשבון שהוא רץ תחתיו.
לכן העבודה מחולקת, וכל צד קורא בדיוק את התיבה שהוא *יכול* לקרוא:

| | רץ איפה | קורא | כותב |
|---|---|---|---|
| `finance_app.gs` | Apps Script, כחשבון **uper.request** | התיבה הראשית | בעלים של הגיליון, הממשק, קבלות מהטלפון |
| `finance_bot.py` | GitHub Actions, פעם ביום | תיבת **האספקה** (IMAP) | מכירות, התאמת ספק, שערי מטבע |

אף אחד מהם לא רואה את התיבה של השני, ואף אחד לא צריך.

> **חשוב:** הבוט פותח את תיבת האספקה ב**קריאה בלבד**. בוט האספקה משתמש
> בסימון `\Flagged` כזיכרון של מה שכבר טיפל בו, ובוט הכספים לא יכול לדרוך
> עליו בטעות.

---

## הפעלה — 5 צעדים, פעם אחת

### 1. פתיחת פרויקט Apps Script

היכנס ל־[script.google.com](https://script.google.com) **בחשבון uper.request**
→ New project.

### 2. הדבקת שני הקבצים

- הדבק את `finance_app.gs` לתוך `Code.gs`
- File → New → **HTML file**, קרא לו בדיוק `finance_ui` (בלי סיומת),
  והדבק לתוכו את `finance_ui.html`

### 3. הרצת `setup()`

בחר בתפריט הפונקציות את `setup` ולחץ Run. תתבקש לאשר הרשאות.

זה יוצר לבד:
- את הגיליון **"Waverole — כספים"** ב-Drive שלך (בבעלותך)
- תיקייה **"Waverole — קבלות"** לתמונות
- שיתוף עם חשבון הבוט, כדי שהצד של הפייתון יוכל לכתוב
- טריגר יומי שסורק את המייל

הקישור לגיליון יופיע ב-Execution log.

### 4. הרצת `backfill()` — פעם אחת

בחר בתפריט הפונקציות את `backfill` ולחץ Run.

הסריקה היומית מסתכלת 120 יום אחורה בלבד. `backfill` סורק **שלוש שנים**
ומכניס את כל ההיסטוריה שקדמה למערכת — מנויים, חשבוניות תשתית, דמי הקמה.

בסוף הריצה יופיע ב-Execution log כמה הוצאות נוספו. **אם המספר גדול (מעל 400),
הרץ שוב** — Apps Script עוצר ריצה אחרי 6 דקות, והסקריפט ממשיך מהמקום שבו
נעצר. אפשר להריץ כמה פעמים שרוצים: כל שורה מזוהה לפי מזהה ההודעה בג'ימייל,
אז אין כפילויות.

כל מה שנסרק נכנס בסטטוס **"לבדיקה"** ולא נספר בספרים עד שתאשר אותו
בלשונית "הוצאות". ככה קבלה שזוהתה בטעות לא מזייפת את הרווח.

### 5. פרסום כאתר

Deploy → New deployment → סוג **Web app**:
- Execute as: **Me**
- Who has access: **Only myself**

הקישור שמתקבל הוא האתר. פתח אותו באייפון והוסף למסך הבית —
הוא נראה ומתנהג כמו אפליקציה.

---

## סריקת קבלות מהטלפון (אופציונלי, לחיצה אחת)

בעורך: Services → **Drive API** → Add.

בלי זה הצילום עדיין נשמר ומקושר להוצאה — פשוט תצטרך להקליד את הסכום.
עם זה, Drive מבצע OCR והמערכת ממלאת סכום, ספק, תאריך וקטגוריה לבד.

---

## עמלות סליקה

כרגע כל התשלומים מתקבלים ב-**Bit**, שלא עולה כלום לקבל.
לכן המערכת רושמת עמלת סליקה **0** — לא הערכה, אפס אמיתי.

> עמודת "סליקה" במחירון היא **תמחור מתוכנן**, לא חיוב שקרה. המערכת
> לא סופרת אותה כהוצאה.

כשיעלו PayMe / PayPal לאתר:

1. ב־`.github/workflows/finance.yml` שנה את `PAYMENT_PROVIDER` ל־`payme`
   או `paypal`
2. או, אם יהיו כמה אמצעי תשלום במקביל — הוסף לגיליון הקבלות עמודה בשם
   **"אמצעי תשלום"**, ותכתוב בה `bit` / `payme` / `paypal` לכל הזמנה.
   המערכת תיקח את העמלה הנכונה לכל שורה לבד.
3. את האחוזים המדויקים לפי ההסכם שלך אפשר לקבוע ב-Actions דרך
   `FEE_RATE_PAYME` / `FEE_RATE_PAYPAL`. עד שזה נעשה, כל שורה כזו מסומנת
   **"(הערכה)"** כדי שלא תבלבל בין מספר שיודעים לבין מספר שהנחנו.
4. אם תגיע קבלה מהספק עם העמלה בפועל, היא גוברת על כל הערכה.

---

## הזמנות פנימיות

הזמנה שנקנתה ולא נמכרה (בדיקה, או חבילה שקנית לעצמך על חשבון החברה)
מסומנת אוטומטית כ**פנימית**: מזהים אותה לפי מחיר מכירה 0 עם מחיר קנייה גדול מ-0.

היא **לא** נספרת כמכירה בהפסד — זה היה מעוות את שולי הרווח של החבילה.
היא נספרת כשורת עלות נפרדת, כי הכסף באמת יצא.

כדי לסמן הזמנה כפנימית: פשוט אפס את עמודת "מכירה" בגיליון הקבלות.

---

## הרצה ידנית

בכל רגע: Actions → **Finance (books)** → Run workflow.

מקומית:

```bash
python finance_bot.py --no-mail --dry-run
```

---

## מה נדרש כדי שהצד של הפייתון ירוץ

הכל כבר קיים ב-GitHub Secrets — לא נדרש שום סוד חדש:

- `GOOGLE_CREDENTIALS_JSON` — חשבון השירות (גם קורא את הגיליונות וגם כותב לספרים)
- `GMAIL_APP_PASSWORD` — סיסמת אפליקציה של waverolesupply

עד שתריץ `setup()`, הריצה היומית מסתיימת בירוק ורק כותבת שאין עדיין לאן לכתוב.
